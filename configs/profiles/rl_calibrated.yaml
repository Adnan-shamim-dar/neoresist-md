# rl_calibrated — weights derived from logistic regression on published trial validation data
# Training data: ott2017
# Date: 2026-04-17
# AUC-ROC on training data: 0.5093
profile_id: rl_calibrated
display_name: RL calibrated
version: "1.0"
description: >
  Logistic-regression calibrated ResistanceLoop profile derived from the validation
  training matrix in backend/validation/artifacts/training_matrix.csv. Escape coefficient was non-negative in this training matrix; calibrated penalty is set to 0.0.
higher_is_better: true
primary_score_column: rl_priority

expression_tpm_cap: 1000.0
blend:
  real_expression_weight: 0.85
  real_ccf_weight: 0.85

weights:
  expression_norm: 0.339377
  presentation: 0.240925
  ccf: 0.419698
  self_dissimilarity: 0.000000
escape_penalty_weight: -0.000000
