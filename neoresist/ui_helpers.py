"""Dash-facing label maps (evidence source → user-readable)."""

EVIDENCE_SOURCE_LABELS: dict[str, str] = {
    "stub": "Cohort model estimate",
    "blended_real": "Blended measured + cohort model",
}
